= subscribe_to "/scenes/#{@scene.id}/waiting"

:javascript
  $(function() {
    PrivatePub.subscribe("/scenes/#{@scene.id}/waiting", function(data) {
      if ( data.marius_says === 'refresh' ) location.reload();
    });
  });

%h1 Waiting for your co-writer

%p Invite him / her to join you at this url:

.well.well-small
  = link_to @join_url, @join_url
